# 🔍 دليل البحث المتقدم عن المراكز الطبية المتخصصة
## Advanced Search Guide for Specialized Medical Centers

---

## نظرة عامة | Overview

نظام البحث المتقدم في "وين أروح" يوفر إمكانيات بحث شاملة عن المنشآت الصحية مع فلاتر متعددة، نظام ترتيب ذكي، مقاييس أداء، وحجز مواعيد مباشر.

The Advanced Search system in "Wain Aroh" provides comprehensive search capabilities for healthcare facilities with multiple filters, intelligent ranking system, performance metrics, and direct appointment booking.

---

## ✨ الميزات الرئيسية | Key Features

### 1. 🎯 **البحث الذكي مع الفلاتر المتعددة**
- **التخصصات الطبية**: قلب، أطفال، جراحة، باطنية، إلخ
- **الموقع الجغرافي**: البحث حسب القرب من موقعك
- **الجهات الصحية**: وزارة الصحة، الحرس الوطني، القطاع الخاص
- **التجمعات الصحية**: تجمع الرياض الأول، الثاني، الثالث
- **التقييم**: فلترة حسب التقييم الأدنى
- **التوفر**: المنشآت المتاحة حالياً
- **الخدمات**: الخدمات المطلوبة

### 2. 📊 **نظام الترتيب الذكي**
- **الأكثر صلة**: حسب درجة التطابق مع احتياجاتك
- **الأقرب**: حسب المسافة من موقعك
- **الأعلى تقييماً**: حسب تقييمات المرضى
- **الأكثر توفراً**: حسب نسبة الإشغال
- **الأفضل أداءً**: حسب مجموعة من المقاييس

### 3. 💯 **مقاييس الأداء والجودة**
- **معدل رضا المرضى**: نسبة الرضا عن الخدمة
- **متوسط وقت الانتظار**: الوقت المتوقع للانتظار
- **معدل إشغال الأسرّة**: مدى توفر الأسرّة
- **معدل المواعيد في الوقت**: الالتزام بالمواعيد
- **معدل إعادة الدخول**: جودة العلاج
- **وقت الاستجابة للطوارئ**: سرعة الاستجابة

### 4. 📅 **حجز المواعيد المباشر**
- **عرض الفترات المتاحة**: لمدة 7 أيام قادمة
- **اختيار الطبيب**: حسب التخصص
- **تأكيد فوري**: رسالة تأكيد بالبريد والهاتف
- **إدارة المواعيد**: عرض، تأكيد، إلغاء

### 5. 🗺️ **معلومات تفصيلية**
- **معلومات الاتصال**: هاتف، طوارئ، بريد إلكتروني
- **الموقع**: عنوان مفصل مع إمكانية التوجيه
- **القدرات**: عدد الأسرّة، الأطباء، المعدات
- **ساعات العمل**: أوقات الدوام
- **التأمين**: شركات التأمين المقبولة

---

## 🛠️ المكونات التقنية | Technical Components

### Backend APIs

#### 1. **Search API** (`/api/search/`)

##### البحث عن المنشآت | Search Facilities
```http
POST /api/search/facilities
Content-Type: application/json

{
  "specialties": ["قلب", "أطفال"],
  "location": {"lat": 24.7136, "lng": 46.6753},
  "max_distance_km": 20,
  "organizations": ["وزارة الصحة"],
  "clusters": ["تجمع الرياض الأول"],
  "min_rating": 4.0,
  "available_now": true,
  "sort_by": "relevance",
  "page": 1,
  "limit": 10
}
```

**Response:**
```json
{
  "success": true,
  "results": [
    {
      "facility": {
        "id": 1,
        "name": "مستشفى الملك فهد",
        "location": {"lat": 24.7136, "lng": 46.6753},
        "specialties": ["قلب", "جراحة"],
        "performance": {
          "overall_rating": 4.5,
          "avg_wait_time_minutes": 30,
          "patient_satisfaction": 90.0
        }
      },
      "relevance_score": 95.5,
      "distance_km": 3.2,
      "is_available": true,
      "estimated_wait_time": 25,
      "matched_specialties": ["قلب"],
      "recommendation_reason": "قريب جداً (3.2 كم) • تقييم ممتاز (4.5/5)"
    }
  ],
  "total_results": 15,
  "page": 1,
  "total_pages": 2,
  "stats": {
    "avg_distance": 5.2,
    "avg_rating": 4.3,
    "avg_wait_time": 32,
    "available_count": 12
  },
  "search_time_ms": 45.2
}
```

##### الحصول على تفاصيل منشأة | Get Facility Details
```http
GET /api/search/facilities/{facility_id}
```

##### مقارنة المنشآت | Compare Facilities
```http
POST /api/search/facilities/compare
Content-Type: application/json

{
  "facility_ids": [1, 2, 3]
}
```

##### الحصول على الفلاتر المتاحة | Get Available Filters
```http
GET /api/search/filters
```

##### البحث عن الأقرب | Search Nearby
```http
POST /api/search/nearby
Content-Type: application/json

{
  "location": {"lat": 24.7136, "lng": 46.6753},
  "radius_km": 10,
  "limit": 5
}
```

#### 2. **Metrics API** (`/api/metrics/`)

##### لوحة تحكم المنشأة | Facility Dashboard
```http
GET /api/metrics/facility/{facility_id}/dashboard
```

**Response:**
```json
{
  "success": true,
  "dashboard": {
    "facility_id": 1,
    "facility_name": "مستشفى الملك فهد",
    "kpis": [
      {
        "name": "معدل رضا المرضى",
        "value": 92.5,
        "unit": "%",
        "target": 90.0,
        "status": "good",
        "trend": "up"
      }
    ],
    "statistics": {
      "total_patients": 250,
      "emergency_cases": 45,
      "avg_wait_time": 28.5,
      "patient_satisfaction": 92.5
    },
    "charts": {
      "patient_flow": {
        "labels": ["السبت", "الأحد", ...],
        "data": [120, 135, 142, ...]
      }
    }
  }
}
```

##### مؤشرات الأداء | KPIs
```http
GET /api/metrics/facility/{facility_id}/kpis
```

##### الحالة الفورية | Real-time Status
```http
GET /api/metrics/facility/{facility_id}/status
```

##### الاتجاهات التاريخية | Historical Trends
```http
GET /api/metrics/facility/{facility_id}/trends?metric=patient_satisfaction&days=30
```

##### مقارنة الأداء | Performance Comparison
```http
POST /api/metrics/compare
Content-Type: application/json

{
  "facility_ids": [1, 2, 3]
}
```

#### 3. **Appointment API** (`/api/appointments/`)

##### الحصول على الفترات المتاحة | Get Available Slots
```http
POST /api/appointments/slots
Content-Type: application/json

{
  "facility_id": 1,
  "specialty": "قلب",
  "start_date": "2024-10-15",
  "days": 7
}
```

**Response:**
```json
{
  "success": true,
  "slots": [
    {
      "datetime": "2024-10-15T10:00:00",
      "date": "2024-10-15",
      "time": "10:00 AM",
      "day_name": "Tuesday",
      "doctor_name": "د. أحمد العمري",
      "specialty": "قلب",
      "available": true
    }
  ],
  "total_slots": 42
}
```

##### حجز موعد | Book Appointment
```http
POST /api/appointments/book
Content-Type: application/json

{
  "facility_id": 1,
  "patient_name": "أحمد محمد",
  "patient_phone": "0501234567",
  "patient_email": "ahmad@example.com",
  "specialty": "قلب",
  "doctor_name": "د. أحمد العمري",
  "appointment_datetime": "2024-10-15T10:00:00",
  "notes": "ملاحظات إضافية"
}
```

**Response:**
```json
{
  "success": true,
  "appointment": {
    "id": 123,
    "facility_name": "مستشفى الملك فهد",
    "patient_name": "أحمد محمد",
    "specialty": "قلب",
    "doctor_name": "د. أحمد العمري",
    "appointment_date": "2024-10-15T10:00:00",
    "status": "pending"
  },
  "confirmation": {
    "success": true,
    "message": "تم إرسال تأكيد الموعد"
  }
}
```

##### الحصول على تفاصيل موعد | Get Appointment
```http
GET /api/appointments/{appointment_id}
```

##### تأكيد موعد | Confirm Appointment
```http
POST /api/appointments/{appointment_id}/confirm
```

##### إلغاء موعد | Cancel Appointment
```http
POST /api/appointments/{appointment_id}/cancel
```

##### مواعيد المريض | Patient Appointments
```http
GET /api/appointments/patient/{phone}
```

##### مواعيد المنشأة | Facility Appointments
```http
GET /api/appointments/facility/{facility_id}?date=2024-10-15
```

---

## 🎨 واجهة المستخدم | User Interface

### صفحة البحث | Search Page

**الموقع**: `/search`

#### المكونات الرئيسية:

1. **شريط البحث**
   - حقل نص للبحث السريع
   - زر الفلاتر
   - زر البحث

2. **لوحة الفلاتر**
   - فلاتر التخصصات (checkboxes)
   - فلاتر الجهات (checkboxes)
   - فلاتر التجمعات (checkboxes)
   - التقييم الأدنى (dropdown)
   - المسافة القصوى (slider)
   - الترتيب حسب (dropdown)
   - خيارات إضافية (متاح الآن، يقبل الطوارئ)

3. **إحصائيات البحث**
   - عدد النتائج
   - متوسط المسافة
   - متوسط التقييم
   - متوسط وقت الانتظار

4. **بطاقات النتائج**
   - اسم المنشأة
   - التقييم (نجوم)
   - حالة التوفر (badge)
   - المسافة ووقت الانتظار
   - التخصصات المطابقة
   - سبب التوصية
   - أزرار الإجراءات:
     - حجز موعد
     - اتصال
     - التوجيه

5. **نافذة التفاصيل (Modal)**
   - معلومات الاتصال
   - الموقع
   - التصنيف
   - القدرات
   - أزرار حجز موعد واتصال

---

## 🔄 سير العمل | Workflow

### 1. البحث عن منشأة | Search for Facility

```
المستخدم يفتح صفحة البحث
    ↓
النظام يحدد الموقع الجغرافي
    ↓
المستخدم يختار الفلاتر (اختياري)
    ↓
المستخدم ينقر "بحث"
    ↓
النظام يطبق الفلاتر ويرتب النتائج
    ↓
عرض النتائج مع الإحصائيات
```

### 2. حجز موعد | Book Appointment

```
المستخدم ينقر "حجز موعد"
    ↓
عرض نافذة التفاصيل
    ↓
المستخدم ينقر "حجز موعد" في النافذة
    ↓
عرض الفترات المتاحة
    ↓
المستخدم يختار فترة
    ↓
المستخدم يدخل بياناته
    ↓
تأكيد الحجز
    ↓
إرسال رسالة تأكيد
```

### 3. عرض الأداء | View Performance

```
المستخدم يختار منشأة
    ↓
عرض مقاييس الأداء
    ↓
عرض الرسوم البيانية
    ↓
مقارنة مع منشآت أخرى (اختياري)
```

---

## 📐 خوارزمية الترتيب | Ranking Algorithm

### حساب درجة الصلة (0-100):

1. **تطابق التخصصات (30 نقطة)**
   - نسبة التخصصات المطابقة من المطلوبة

2. **القرب (25 نقطة)**
   - ≤ 5 كم: 25 نقطة
   - ≤ 10 كم: 20 نقطة
   - ≤ 20 كم: 15 نقطة
   - ≤ 30 كم: 10 نقطة
   - > 30 كم: تناقص تدريجي

3. **التقييم (20 نقطة)**
   - (التقييم / 5) × 20

4. **التوفر (15 نقطة)**
   - إشغال < 80%: 15 نقطة
   - إشغال < 90%: 10 نقاط
   - إشغال ≥ 90%: 5 نقاط

5. **وقت الانتظار (10 نقاط)**
   - ≤ 15 دقيقة: 10 نقاط
   - ≤ 30 دقيقة: 7 نقاط
   - ≤ 60 دقيقة: 5 نقاط
   - > 60 دقيقة: 2 نقطة

---

## 🎯 أمثلة الاستخدام | Usage Examples

### مثال 1: البحث عن عيادة قلب قريبة

```javascript
// Frontend Code
const searchFilters = {
  specialties: ["قلب"],
  location: { lat: 24.7136, lng: 46.6753 },
  max_distance_km: 10,
  min_rating: 4.0,
  sort_by: "distance"
};

const response = await fetch('http://localhost:5000/api/search/facilities', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(searchFilters)
});

const data = await response.json();
console.log(data.results);
```

### مثال 2: حجز موعد

```javascript
// Frontend Code
const appointmentData = {
  facility_id: 1,
  patient_name: "أحمد محمد",
  patient_phone: "0501234567",
  patient_email: "ahmad@example.com",
  specialty: "قلب",
  doctor_name: "د. أحمد العمري",
  appointment_datetime: "2024-10-15T10:00:00",
  notes: "فحص دوري"
};

const response = await fetch('http://localhost:5000/api/appointments/book', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(appointmentData)
});

const data = await response.json();
console.log(data.appointment);
```

### مثال 3: عرض مقاييس الأداء

```javascript
// Frontend Code
const facilityId = 1;

const response = await fetch(`http://localhost:5000/api/metrics/facility/${facilityId}/dashboard`);

const data = await response.json();
console.log(data.dashboard.kpis);
console.log(data.dashboard.charts);
```

---

## 📊 قاعدة البيانات | Database Schema

### Models

#### FacilityProfile
```python
@dataclass
class FacilityProfile:
    id: int
    name: str
    type: str
    location: Dict[str, float]
    specialties: List[str]
    services: List[str]
    performance: PerformanceMetrics
    # ... المزيد من الحقول
```

#### PerformanceMetrics
```python
@dataclass
class PerformanceMetrics:
    overall_rating: float
    avg_wait_time_minutes: int
    patient_satisfaction: float
    current_occupancy: float
    # ... المزيد من المقاييس
```

#### Appointment
```python
@dataclass
class Appointment:
    id: int
    facility_id: int
    patient_name: str
    patient_phone: str
    specialty: str
    doctor_name: str
    appointment_date: datetime
    status: str
    # ... المزيد من الحقول
```

---

## 🔐 الأمان | Security

### اعتبارات الأمان:

1. **التحقق من البيانات**
   - التحقق من صحة جميع المدخلات
   - منع SQL Injection
   - منع XSS

2. **الخصوصية**
   - تشفير البيانات الحساسة
   - عدم عرض معلومات المرضى للعامة

3. **المصادقة**
   - التحقق من هوية المستخدم قبل الحجز
   - استخدام tokens للجلسات

4. **الحد من الطلبات**
   - Rate limiting لمنع الإساءة
   - Throttling للطلبات المتكررة

---

## 🚀 التحسينات المستقبلية | Future Enhancements

### المخطط لها:

1. **خرائط تفاعلية**
   - عرض المنشآت على خريطة
   - التوجيه المباشر

2. **التقييمات والمراجعات**
   - تقييمات المرضى
   - تعليقات ومراجعات

3. **الدفع الإلكتروني**
   - دفع رسوم الحجز
   - دفع رسوم الاستشارة

4. **الإشعارات الذكية**
   - تذكير بالمواعيد
   - تحديثات الحالة

5. **التكامل مع HIS**
   - ربط مع أنظمة المستشفيات
   - مزامنة البيانات

6. **الذكاء الاصطناعي المتقدم**
   - توصيات شخصية
   - تنبؤ بأوقات الانتظار

---

## 📞 الدعم الفني | Technical Support

للاستفسارات والدعم الفني:
- **البريد الإلكتروني**: support@wain-aroh.sa
- **الهاتف**: 920000000

---

**نسخة الدليل**: 1.0  
**تاريخ التحديث**: 14 أكتوبر 2024  
**الحالة**: ✅ جاهز للاستخدام

